source 'https://rubygems.org'

gem 'zeitwerk'
gem 'colorize'
gem 'ostruct'

group :build do
    gem 'erubi'
end

group :serve do
    gem 'debug'
    gem 'sinatra'
    gem 'rackup'
    gem 'puma'
    gem 'listen'
end
