source 'https://rubygems.org'

gem 'zeitwerk'
gem 'colorize'
gem 'ostruct'

group :build do
    gem 'erubi'
    gem 'debug'
end

group :serve do
    gem 'rackup'
    gem 'sinatra'
    gem 'puma'
    gem 'listen'
    gem 'debug'
end
